return {
  -- {
  --   "saghen/blink.cmp", -- Assuming you are using blink.cmp, which is common in LazyVim
  --   opts = function(_, opts)
  --     opts.keymap = {
  --       preset = "super-tab", -- This preset configures Tab for cycling and accepting
  --       ["<Tab>"] = { "select_and_accept" }, -- Explicitly set Tab to select and accept
  --       ["<S-Tab>"] = { "select_prev" }, -- Shift+Tab to select previous item
  --     }
  --   end,
  -- },
}
